import { AutoInputWidthDirective } from './auto-input-width.directive';

describe('AutoInputWidthDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoInputWidthDirective();
    expect(directive).toBeTruthy();
  });
});
